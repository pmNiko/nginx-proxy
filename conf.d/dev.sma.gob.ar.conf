server {
    listen 80;
    server_name dev.sma.gob.ar;

    # Configuración de logs específicos
    access_log /var/log/nginx/dev.sma.gob.ar.access.log;
    error_log /var/log/nginx/dev.sma.gob.ar.error.log;

    # Configuraciones de seguridad básicas
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;

    # DNS resolver para Docker
    resolver 127.0.0.11 ipv6=off;

    # 🚧 Fallback para mantenimiento
    location @fallback {
        root /usr/share/nginx/html;
        try_files /fallback-assets/maintenance.html =503;
        
        add_header Content-Type text/html;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
        add_header Expires "0";
        add_header Retry-After "30";
        add_header X-Maintenance-Mode "true";
    }

    # 📁 Assets de fallback
    location /imgs/ {
        alias /usr/share/nginx/html/fallback-assets/;
        try_files $uri $uri/ =404;
    }

    # Health check endpoint
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }

    # Incluir todas las ubicaciones definidas modularmente
    include /etc/nginx/conf.d/locations/*.conf;
}